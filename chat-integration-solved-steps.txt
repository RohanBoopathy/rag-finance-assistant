Solved Steps - AI Assistant + Sidebar + ChatHistory Integration

1) Fixed backend conversation controller issues that blocked history APIs:
- getConversations now accepts userId from query/body.
- getConversationById now uses correct route param: conversationId.
- Removed invalid stray characters in response line.
- createConversation now uses new Conversation(...) + await save().
- addConversation now reads role/content from request body.

2) Fixed Sidebar API wiring and logic:
- Corrected API base from /api/chat to /api.
- Sidebar now fetches history from GET /api/conversations?userId=...
- Removed unrelated message/send logic from Sidebar.
- Delete now correctly filters by _id (not id) and calls backend delete API.
- ChatHistory loading state now uses real loadingConversations value.

3) Connected Sidebar actions to AI page with events:
- On history click: dispatch ai-chat:select-conversation with conversationId.
- On new chat: dispatch ai-chat:new-conversation.
- On delete/send: dispatch ai-chat:refresh-conversations.

4) Updated ai-assistant page to sync with Sidebar:
- Uses session user id (_id/id) and access token.
- Sends chat requests with Authorization Bearer token.
- Sends conversationId when continuing an existing chat.
- Loads selected conversation from /api/conversations/:id?userId=...
- Clears current chat on ai-chat:new-conversation event.

5) Fixed ChatHistory UI bugs:
- Corrected class typo p-x-3 -> px-3.
- Reduced loading spinner container height for sidebar fit.

6) Validation:
- Frontend build passed: npm run build (frontend).
- Backend syntax check passed: node --check src/controllers/conversationController.js.
